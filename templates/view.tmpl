<!DOCTYPE html>
<html>
<body>
    <h1>{{.title}}</h1>
    <ul>
    {{range .people}}
        <li>Name: {{.Name}} - Age: {{.Age}} - Occupation: {{.Occupation}}</li>
    {{end}}
    </ul>
</body>
</html>